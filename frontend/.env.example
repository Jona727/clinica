# URL base de la API del backend (incluye /api al final).
# En desarrollo local, si no se define, usa http://localhost:3000/api.
# En producción, apuntar al backend real (InfinityFree no puede correrlo:
# ver README para dónde hostearlo).
VITE_API_URL=http://localhost:3000/api
